name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: stable
      - name: Check formatting
        run: |
          out=$(gofmt -l .)
          if [ -n "$out" ]; then echo "unformatted:"; echo "$out"; exit 1; fi
      - name: Version file must match the tag
        # mpdtui embeds internal/version/VERSION into the binary, so a tag
        # whose VERSION file was not bumped would ship a binary that reports
        # the wrong version. Catch it before anything is published.
        run: |
          tag="${GITHUB_REF_NAME#v}"
          file=$(tr -d '[:space:]' < internal/version/VERSION)
          if [ "$tag" != "$file" ]; then
            echo "tag $GITHUB_REF_NAME disagrees with internal/version/VERSION ($file)"
            echo "bump the file and re-tag, or tag v$file"
            exit 1
          fi
      - run: go vet ./...
      - run: go test ./...

  release:
    # Never publish a tag whose tests do not pass.
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          # GoReleaser builds the changelog from history, so a shallow clone
          # would produce an empty one.
          fetch-depth: 0
      - uses: actions/setup-go@v7
        with:
          go-version: stable
      # Fail loudly and early rather than after the GitHub release is already
      # published — without this the release succeeds and the tap silently
      # stays stale on a 401.
      - name: Require tap token
        run: |
          if [ -z "${{ secrets.HOMEBREW_TAP_TOKEN }}" ]; then
            echo "HOMEBREW_TAP_TOKEN is not set on this repo."
            echo "Set it with: gh secret set HOMEBREW_TAP_TOKEN --repo ${{ github.repository }}"
            exit 1
          fi
      - uses: goreleaser/goreleaser-action@v7
        with:
          distribution: goreleaser
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # A PAT with contents:write on susamn/homebrew-tap. The default
          # GITHUB_TOKEN cannot push to another repository.
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
